<h2>Cocktails</h2>
<ul class="list-group">
  <% @cocktails.each do |cocktail| %>
  <li> <%= link_to cocktail.name, cocktail_path(cocktail) %> </li>
  <% end %>
</ul>

<%= link_to "Create", new_cocktail_path, class:"btn btn-secondary w-50 "%>
</div>
</div>
